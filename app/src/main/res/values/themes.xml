<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HotPlaceContactApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HotPlaceContactApp" parent="Base.Theme.HotPlaceContactApp" >
        <item name="android:statusBarColor" tools:targetApi="l">@color/orange</item>        <!-- 상태바 배경색 tool부분은 라이트모드,다크모드 선택할때 추가하는 부분 -->
        <item name="android:windowLightStatusBar">true</item>                   <!-- 상태바 아이콘 색 true가 흰색 false가 검은색 -->

        <item name="colorPrimary">#f5a623</item>                                <!-- 배경 -->

        <item name="android:textColorPrimary">@android:color/white</item>       <!-- 탭바 선택된 텍스트 -->
        <item name="android:textColorSecondary">@android:color/black</item>     <!-- 탭바 텍스트 -->

        <item name="colorAccent">@android:color/white</item>                    <!-- 하단 움직이는 바 색-->

    </style>



    <!-- splash screen Theme -->
    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.HotPlaceContactApp</item>


    </style>
</resources>